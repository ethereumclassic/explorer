const assert = require('assert');

//* load unit tests *//

const app = require('./units/apps');

//* end unit tests *//

/* tests to run (add tests here)  */

describe('Global functions', () => {
  app;
});
